import React from "react";
import { Box, Drawer, Typography, ListItem, Button, Menu, MenuItem, ListItemIcon, ListItemText } from "@mui/material";


const Payments = () => {
  return (
    <div>
      <Typography>Rergistro pagos</Typography>
    </div>
  );
};

export default Payments;